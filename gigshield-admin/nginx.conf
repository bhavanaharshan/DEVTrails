server {
    listen 4174;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Deny access to .env and hidden files
    location ~ /\. {
        deny all;
    }
}
