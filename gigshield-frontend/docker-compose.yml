
services:
  gigshield-frontend:
    build: .
    container_name: gigshield-ui
    ports:
      - "5173:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true # Helps Vite hot-reload work on Windows
    # Note: She will need to provide her own .env file locally for Firebase to work